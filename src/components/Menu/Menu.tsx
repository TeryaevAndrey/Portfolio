import React, { FC } from 'react';
import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 26px;
`;

const Menu: FC = () => {
  return (
    <div>
      
    </div>
  );
};

export default Menu;